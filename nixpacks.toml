[phases.setup]
nixPkgs = ["...", "php83Packages.composer", "nodejs_20"]

[phases.build]
dependsOn = ["setup"]
cmds = ["composer install --no-dev", "npm install", "npm run build"]

[start]
cmd = "php artisan migrate --force && apache2-foreground"
